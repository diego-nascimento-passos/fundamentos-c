#include <stdio.h>

int main (){

    // Declarando a variável contador
    int contador;

    // Estrutura de repetição For
    for (contador = 1; contador <= 10; contador++)
    printf ("%d\n", contador);


    return 0;
}