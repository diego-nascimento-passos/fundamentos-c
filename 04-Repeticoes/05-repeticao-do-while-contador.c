#include <stdio.h>

int main (){

    // Declaração de variáveis 
    int contador = 1;

    // Repetição do-while
    do {
        printf ("%d\n", contador);
        contador++;
    }while (contador <= 5);

    return 0;
}